import styled from "styled-components";

function sample() {
  const Sample = styled.section`
    position: relative;
    color: #878787;
  `
  return (
    <Sample>
      Sample
    </Sample>
  );
}

export default sample;
