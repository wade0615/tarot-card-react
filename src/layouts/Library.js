import React, { useState } from "react";
import styled from "styled-components";

const StyleLibrary = styled.section`
  position: relative;
  color: #878787;
`;

function Library() {
  return <StyleLibrary>Library</StyleLibrary>;
}

export default Library;
